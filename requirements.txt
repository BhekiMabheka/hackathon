# Streamlit Cloud uses this file instead of pyproject.toml / Poetry.
# Only the dashboard dependencies are listed here — the full ML stack
# is installed separately for local development via: pip install -e ".[dev]"

streamlit>=1.35
pandas>=2.1
numpy>=1.26
pyarrow>=14.0
scikit-learn>=1.4
lightgbm>=4.3
shap>=0.45
matplotlib>=3.8
plotly>=5.20
joblib>=1.3
